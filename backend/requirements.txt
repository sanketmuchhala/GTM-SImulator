# GTM Simulation Lab — Backend Dependencies
# Python 3.11+ required

# ── Core framework ─────────────────────────────────────────────
flask>=3.0.0
flask-cors>=6.0.0

# ── Production WSGI server ─────────────────────────────────────
gunicorn>=21.0.0

# ── LLM (OpenAI-compatible, works with OpenRouter) ────────────
openai>=1.0.0

# ── Memory (Zep — only used by legacy OASIS flow) ─────────────
zep-cloud==3.13.0

# ── File processing ────────────────────────────────────────────
PyMuPDF>=1.24.0
charset-normalizer>=3.0.0
chardet>=5.0.0

# ── Utilities ──────────────────────────────────────────────────
python-dotenv>=1.0.0
pydantic>=2.0.0
